{% extends "base.html.twig" %}

{% block title %}Admin Création Utilisateur
{% endblock %}

{% block body %}
<section class="form">
    <div class="wrapper">
        <h1 class="form-title">Création d'utilisateur</h1>
                
        {{ form_errors(form) }}
        {{ form_start(form) }}            
            <div class="input-box">
                {{ form_row(form.lastname) }}
            </div>
            <div class="input-box">
                {{ form_row(form.firstname) }}
            </div>
            <div class="input-box">
                {{ form_row(form.username) }}
            </div>
            <div class="input-box">
                {{ form_row(form.email) }}
            </div>
            <div class="input-box">
                {{ form_row(form.address) }}
            </div>
            <div class="input-box">
                {{ form_row(form.zipcode) }}
            </div>
            <div class="input-box">
                {{ form_row(form.city) }}
            </div>
            <div class="input-box">
                {{ form_row(form.plainPassword) }}
            </div>
            <div class="input-box">
                {{ form_row(form.confirmPassword) }}
            </div>
            <button class="btn" type="submit">
                Créer l'utilisateur
            </button>
           
            {{ form_end(form) }}
        
    </div>  
</section>
{% endblock %}